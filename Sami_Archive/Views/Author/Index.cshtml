@model AuthorsListViewModels

<div>
    <a class="btn btn-success btn-sm pull-right text-black" asp-controller="Author" asp-action="Create">Create Author</a>
</div>

@foreach (var p in Model?.Authors ?? Enumerable.Empty<Author>())
{
    <partial name="AuthorSummary" model="p" />
}


<div page-model="@Model?.PagingInfo" page-action="Index" page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary" class="btn-group pull-right m-1">
</div>