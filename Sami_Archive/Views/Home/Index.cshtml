@model BooksListViewModels

@foreach (var p in Model?.Books ?? Enumerable.Empty<Sami_Archive.Models.Book>())
{
    <div>
        <h3>@p.Title</h3>
        <p>@p.Description</p>
        <p>@p.Genre</p>
    </div>
}